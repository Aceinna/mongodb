<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>MY APP</title>
</head>
<body>

<ul class="quotes">
  <% for(var i=0; i<Math.min(quotes.length,100); i++) {%>
    <li class="quote">
        <span><%= i %></span>
      <span><%= quotes[i].staid %></span>
      <span><%= quotes[i].sec_gaps %></span>
    </li>
  <% } %>
</ul>

</body>
</html>